streamlit==1.30.0
altair==5.0.1
pandas==2.2.0
# Note: numpy removed to allow Streamlit Cloud to resolve a compatible wheel automatically.
# If your app requires NumPy/SciPy, consider using Docker with the necessary system dependencies.
